<div class="row">
  <div class="col">
  <h3>Base:</h3><p>https://statsapi.web.nhl.com</p>
    <ul class="list-unstyled">
      <% @configs.each do |config| %>
        <li class="mb-3">
          <strong><%= "#{config['description']}: " %></strong> <%= "#{config['endpoint']}"  %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
